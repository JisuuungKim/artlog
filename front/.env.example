# 로컬 개발용 백엔드 베이스 URL
VITE_API_BASE_URL=http://localhost:8080

# 운영(Vercel)에서는 빌드 타임에 Vercel 프로젝트 환경변수로 아래 값을 주입합니다.
#   VITE_API_BASE_URL=https://api.artlog.site
# (코드에 직접 넣지 말고 Vercel 대시보드 → Settings → Environment Variables 에 등록)
