# ─── Web Framework ───────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# ─── LangGraph & LangChain ───────────────────────────────────
langgraph>=0.2.56
langchain-core>=0.3.31
langchain-openai>=0.2.14
langchain-community>=0.3.15     # TavilySearchResults 등 커뮤니티 도구
langchain-teddynote>=0.1.0      # 그래프 시각화 (visualize_graph)

# ─── OpenAI ──────────────────────────────────────────────────
openai>=1.30.0                  # Whisper API (client.audio.transcriptions)

# ─── Audio ───────────────────────────────────────────────────
# 오디오 청크 분할/다운샘플은 ffmpeg(+ffprobe) CLI 를 직접 호출합니다 (stt_agent.py).
# 전체 PCM 을 메모리에 올리지 않기 위해 pydub 대신 ffmpeg 스트리밍 처리를 사용합니다.

# ─── Search Tool ─────────────────────────────────────────────
tavily-python>=0.3.9            # TavilySearchResults 의존성

# ─── PostgreSQL (psycopg3, pgvector) ─────────────────────────
psycopg[binary,pool]>=3.2.3
pgvector>=0.3.0

# ─── Settings & Env ──────────────────────────────────────────
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
