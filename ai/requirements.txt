# ─── Web Framework ───────────────────────────────────────────
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0

# ─── LangGraph & LangChain ───────────────────────────────────
# 코드가 0.x API 기준으로 작성됨. 상한을 둬서 1.x 자동 업그레이드로 인한
# 런타임 API 깨짐을 막고 빌드 재현성을 확보한다.
langgraph>=0.2.56,<0.3
langchain-core>=0.3.31,<0.4
langchain-openai>=0.2.14,<0.4
langchain-community>=0.3.15,<0.4     # TavilySearchResults (correction_node 가사 검색)

# ─── OpenAI ──────────────────────────────────────────────────
openai>=1.30.0,<2.0                  # Whisper / gpt-4o-transcribe-diarize, embeddings

# ─── Search Tool ─────────────────────────────────────────────
tavily-python>=0.3.9,<1.0            # TavilySearchResults 의존성

# ─── Audio ───────────────────────────────────────────────────
# 오디오 청크 분할/다운샘플은 ffmpeg(+ffprobe) CLI 를 직접 호출합니다 (stt_agent.py).

# ─── PostgreSQL (psycopg3, pgvector) ─────────────────────────
psycopg[binary,pool]>=3.2.3,<4.0
pgvector>=0.3.0,<1.0

# ─── Settings & Env ──────────────────────────────────────────
pydantic>=2.10.0,<3.0
pydantic-settings>=2.6.0,<3.0
python-dotenv>=1.0.1,<2.0
